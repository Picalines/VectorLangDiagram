@using VectorLang.Model

<!-- TODO: actual render -->
<p>@Vector.Label: @Vector.Coordinates @Vector.Color</p>

@code {
    [Parameter]
    public PlottedVector Vector { get; set; } = new((1, 1), "label", (1, 1, 1));
}
